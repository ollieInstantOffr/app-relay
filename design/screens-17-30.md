# Design notes — screens 17–30

The Design MCP truncates `Relay Proxy Manager.dc.html` at 256 KiB, so the local
copy ends inside screen 16d. This file holds the text content of the remaining
screens, extracted from the full design. Visual language is identical to
screens 01–16 (see `brand/tokens.md`). Fields are separated with `|`.

## 17 Config history
Dark pending bar at the top of the app: "3 pending changes | grafana.home.lan edited · backend api: server added · lan-only: rule added | Review diff | Discard | Apply & reload".
Page "Config history" · badge "v142 live" · subtitle "Every apply = a version. Rollback re-validates before swapping."
Left list of versions: v142 live · 2h ago · admin · "Backend api: leastconn, health check /healthz" | v141 · Yesterday · mcp:claude · "Drain server 10.0.0.92 (minio)" | v140 · **auto rolled back** (amber) · Yesterday · admin · "cloud.home.lan → 10.0.0.35 · post-reload health check failed, reverted to v139 in 4 s" | v139 · 2 days ago · admin · New host grafana.home.lan | v138 · 3 days ago · docker · Auto-created host loki.home.lan from labels | v137 · 5 days ago · admin · Certificate renewed *.home.lan.
Right: diff "v142 vs v141 | haproxy.cfg · +3 −2 · validated in 38 ms · reload dropped 0 connections | Download | Roll back to v141", unified diff with line numbers (red removed / green added lines).
"Apply pipeline" strip: Render → Validate (nginx -t · haproxy -c) → Atomic swap → Reload → Health check 10 s · "fails anywhere → previous version restored".
Toast (warning): "Change rolled back automatically | cloud.home.lan returned 502 for 10 s after reload. v139 is live again; your edit is saved as a draft. | Open draft | View logs | ×".

## 18a Host drawer — Locations tab
"Edit host cloud.home.lan" tabs Details | SSL | Locations | Advanced. "Route paths to different upstreams or add headers. `/` is the host's default upstream. Longest prefix wins."
Rows (draggable): `/` → https://10.0.0.30:443 (default) · `/office/` → http://10.0.0.36:9980 expanded (▴ collapse): scheme select, host, port, toggles Websockets / Strip prefix / Cache / Different access list; "Extra request headers" X-Forwarded-Prefix = /office (×) + Add header · `/.well-known/` → same upstream · no auth · `/metrics` → 403 deny · "+ Add location".
Preview code: `location /office/ { proxy_pass http://10.0.0.36:9980; proxy_set_header X-Forwarded-Prefix /office; proxy_set_header Upgrade $http_upgrade; }` · "Config valid" · Cancel · **Save to pending**.

## 18b Host drawer — Advanced tab
Authentication: "Forward-auth to an SSO provider, in addition to the access list" · provider select (Authelia) · Verify URL `http://10.0.0.5:9091/api/verify?rd=https://auth.home.lan` · toggles Pass Remote-User, Pass Remote-Groups, Skip for /.well-known/.
Rate limiting: "Per client IP · 429 when exceeded" · Requests 30 / s · Burst 60 · Exempt (access list select lan-only).
Max upload size 10 GB · Proxy timeouts read 300s · send 300s.
Custom nginx snippet: "Escape hatch · inserted into the server block · validated on save" · badge advanced · code editor `add_header X-Robots-Tag "noindex" always; proxy_hide_header X-Powered-By;` · hint "# Ctrl+Space for directive completion" · Config valid · Cancel · Save to pending.

## 19a Load balancer — Frontends
Tabs Backends | Frontends | Stats · "+ New frontend". Intro: "A frontend is a listening port plus rules that pick a backend. Frontends created by the Expose wizard bind to localhost and are reached only through the reverse proxy."
Card http-in · bind 127.0.0.1:10080 · HTTP · badge "via reverse proxy" · 342 sess/s · ··· ; rules table (# | If | Then use backend): 1 host = api.home.lan and path starts /v1 → api (Edit) · 2 host = s3.home.lan → minio · 3 otherwise → web-app · + Add rule.
Card pg-in · bind 10.0.0.1:5433 · TCP → postgres-ro · "LAN bind · not proxied" · 11 conn.
Card tls-passthrough · bind 0.0.0.0:8443 · TCP · SNI routing · PROXY protocol · 2 rules · 4 sess/s.
Model callout: "nginx owns public TLS and domains. HAProxy owns pools and health. Public HTTP traffic always enters through nginx; only raw TCP/UDP frontends bind to a LAN or public IP directly."

## 19b Load balancer — Stats
"Live · 2 s refresh" · "Last 1h ▾". Stat cards: Sessions / s (now 342 · peak 1.1k, sparkline) · Errors · 1h 31 (27 conn · 4 resp · 0 req) · Queue · retries 0 · 12 (redispatched 3).
Table: Backend / server | Status | Sess/s | Current | Max | Queue | Errors | Resp p95 | Uptime. Backend rows bold (web-app UP 210 84 612 0 2 41 ms 41d) with indented server rows (10.0.0.71:8080 UP …). api DEGRADED; 10.0.0.82:9000 DOWN 0 0 188 0 27 — "6m ↓". postgres-ro UP, minio UP.

## 20 Redirects & default host (Proxy hosts page, tabs Hosts · 18 | Redirects · 3 | Default)
"+ New redirect". Table From | To | Code | Options: www.example.com → https://example.com$request_uri 301 "keep path · TLS" · wiki.home.lan → https://docs.home.lan/wiki/ 302 "drop path · TLS" · home.lan/old-blog → https://blog.example.com 308 "keep path".
Default host card: "What a request for an unknown domain (or a bare IP) gets. Also answers HTTP-01 challenges." Radio: Close connection (444) "Nothing leaks · recommended when public" · Relay 404 page "Neutral, unbranded" · Redirect to [https://home.lan] · Serve a proxy host [Pick an existing host].
Default TLS certificate: "Presented when SNI matches no host. Self-signed keeps scanners from learning your domains." Radio Self-signed placeholder "CN=localhost · regenerated yearly" · *.home.lan "Let's Encrypt · 88 days". Footer "Unknown-host hits last 24 h: 1,204 · 98% scanners".

## 21 Audit log (Logs page tabs Access | Error | Audit | Approvals (badge 2))
Export CSV · filter "actor, action, target…" · All actors ▾ · Last 7 days ▾.
Table Time | Actor | Action | Target / detail | Version | Result. Actor chips: "AI" dark chip + "mcp · Claude Desktop"; user avatar initials "jonas"; "SYS" chip "system"/"docker". Results: ok, pending (amber), applied, denied (red), auto, reverted, blocked.
Rows e.g. 14:02:11 mcp · Claude Desktop query_logs "host=jellyfin status≥500 · 1h" ok · 13:58:40 drain_server "minio / 10.0.0.92 · awaiting approval" pending · 12:10:02 jonas host.update "grafana.home.lan · websockets on" v142 applied · 11:47:19 jonas auth.login "192.168.1.24 · 2FA passkey" ok · delete_host "old-wiki.home.lan · tool disabled for token" denied · system config.rollback "v140 → v139 · health check failed (cloud.home.lan 502)" v139 auto · host.update reverted · mira auth.login_failed "203.0.113.88 · wrong password ×3 · IP throttled 15 min" blocked · docker host.create "loki.home.lan · from labels on container loki" v138 applied.
Right panel "Pending approvals · Write tools wait here up to 10 min": cards with AI chip, tool name, client, age, description ("Drain 10.0.0.92:9000 in backend minio. Reason given: "…""), buttons Approve (dark) · Deny · Preview change.

## 22 Empty / error states
- 22a Empty hosts: "No proxy hosts yet | A host maps a domain to something on your network. Start with one, or pull them in from Docker or an existing Nginx Proxy Manager. | + New host | From Docker · 6 found | Import NPM".
- 22b Docker not detected (Settings · Docker discovery): "Docker socket not found | Looked for /var/run/docker.sock. Mount it into the Relay container, or point at a remote Docker host over TCP." code `volumes: - /var/run/docker.sock:/var/run/docker.sock:ro` · Security note: "Access to the Docker socket is equivalent to root on the host. Mount it read-only, or use a socket proxy that only allows GET /containers." · Retry detection · Connect remote host…
- 22c nginx down: red banner "nginx is not running | All 18 hosts unreachable · exited 40 s ago | Start nginx | Error log"; overview cards show "18 down", "— no data since 14:22", "Load balancer 4 haproxy fine"; error card `14:22:07 [emerg] bind() to 0.0.0.0:443 failed (98: Address already in use)` · "Likely cause: another process holds port 443." · Show listeners.
- 22d HAProxy failed to start: "HAProxy failed to start after apply v143 | Validation passed but the process exited on startup. v142 was restored automatically and is serving traffic. Your change is kept as a draft." code `[ALERT] Binding for frontend pg-in: cannot bind socket (Address already in use) [10.0.0.1:5433]` · Open draft · Compare v142 → v143 · Discard draft · hint "Port 5433 is used by the postgres stream on the reverse proxy — pick another port or remove the stream."
- 22e Database locked: "Relay can't reach its database | /data/relay.db is locked or read-only. nginx and HAProxy keep running with the last applied config — nothing is down, but changes can't be saved. | Retry | Diagnostics".
- 22f No backends: "No load-balancer backends | Group two or more servers into a pool with health checks. HAProxy starts only once you create the first backend. | + New backend | Convert a host's upstream into a pool".

## 23 Components
Toasts (bottom-right stack, newest on top, 6 s, errors persist, 420 px): success "Host saved · grafana.home.lan added to pending changes. · Apply now"; progress "Applying v143… · Validated · reloading nginx · health check 4/10 s" with bar; error "Certificate request failed · DNS-01 for vault.home.lan: TXT record not visible after 120 s…· Retry · Open provider"; warning "Server 10.0.0.82 is down · backend api · L7 timeout · traffic rerouted · View"; undo "Host deleted · Undo · 5s"; approval (dark) "AI · Claude Desktop wants to drain a server · minio / 10.0.0.92 · expires in 9 min · Approve · Deny · Details".
Dialogs (centered 440–520 px, scrim rgba(20,20,20,.32), radius 14):
- Delete cloud.home.lan? "The host and its 2 locations are removed on next apply. Traffic to this domain will get the default-host response (444)." checkbox "Its certificate cloud.home.lan is used by nothing else. Delete it too" · "Type the domain to confirm" input · Cancel · Delete host (red).
- Request certificate: "Let's Encrypt · production" · Domains chips *.home.lan home.lan · Challenge radio DNS-01 · Cloudflare "Required for wildcards · works behind CGNAT" / HTTP-01 "Unavailable — wildcard domains need DNS-01" (disabled) · checkbox "Use staging first (no rate limits, browser-untrusted)" · "LE limit: 50 certs / domain / week · 3 used" · Cancel · Request.
- Set 10.0.0.92 to drain: "New sessions go to other servers; existing ones finish. The server is marked MAINT when idle or after the timeout." Grace timeout 5 min · Active sessions 4 · ~40 s to finish · Cancel · Drain.
- Forgot your password? "Relay doesn't assume you have email set up. Reset from the machine it runs on:" code `docker exec -it relay relay users reset-password jonas` · "If SMTP is configured, you can also send a reset link." · Back to sign in.
Menus: host menu (jellyfin.home.lan header) Edit E · Open in new tab ↗ · Test upstream now · View logs for host · Duplicate · Disable · Delete… ⌫ (red). Server menu (server 10.0.0.82): Set weight… · Drain (graceful) · Maintenance (immediate) · Mark as backup · Run health check · Remove from pool (red).
Command palette ⌘K: input "jelly" esc · group Hosts: jellyfin.home.lan → 10.0.0.42:8096 ↵ · group Actions: Edit jellyfin.home.lan · Show logs · host:jellyfin.home.lan · Test upstream 10.0.0.42:8096 · group Navigate: Go to Certificates · Load balancer · Settings…
Tooltip: dark bubble "Let's Encrypt · expires in 88 d · auto-renews" over chip "TLS · LE", 300 ms delay.
Field states: Default (placeholder 10.0.0.x) · Focus (ink border + ring) · Invalid "10.0.0.300" red border + "Not a valid IPv4 address" · Verifying "Checking upstream…" spinner.
Status badges: healthy (green) · degraded (amber) · down (red) · disabled (grey) · pending (amber outline) · draining (blue-grey).
Buttons: Primary (ink) · Secondary (white, hairline) · Ghost · Destructive (red) · Disabled · Saving… (spinner).
Rules: "Drawers (560/640 px, right) for anything with a form or tabs. Dialogs for confirmations and short single-purpose forms. Toasts never require action except the Undo and approval variants. Destructive actions: red button + typed confirmation when the target has dependents."

## 24 Brand and icons
Logo mark "source → relayed": filled origin node, link, open destination node (brand/logo-mark.svg). Green variant only for an "all healthy" tray icon. Wordmark Geist 600, −0.03em.
Favicon: heavier strokes below 32 px (brand/favicon.svg). Tab title states: "Relay · Overview", "(1) Relay · jellyfin down", "Relay · 3 pending". theme_color #141414.
Nav icons (rail order): overview, hosts, load-balancer, certificates, access, streams, logs, history, settings (+ users, mcp, docker, redirect, expose, terminal, stats). Action icons: plus search close chevron more drag edit trash copy external reload check warning info filter download upload drain power reveal token link bolt(AI) rollback.
Rail: 18 px icons, #666 → #141414 on hover, inverted (ink bg, white icon) when active. Hover tooltip shows name + shortcut ("Load balancer G L"). Badges: amber dot = degraded inside, red dot = down, number = pending items (history: unapplied changes; logs: approvals).

## 25 Expose wizard steps 3–4 (820 px modal; stepper Backend · Domain & TLS · Access · Review)
Step 3 "Who may reach app.example.com, and how hard it is to abuse." Who can reach it: radio "Anyone on the internet — It's a public app — protect it with the options below" / "Access list — e.g. admin-vpn · family · lan-only [choose ▾]". Toggles: Sign-in required (forward-auth) "Authelia at auth.home.lan" · Rate limit "30 req/s per IP, burst 60" · Block exploits "Common scanner paths" · Geo-block "Allow only selected countries" · Hide from search engines "X-Robots-Tag: noindex". Warning: "Public with no sign-in. Make sure the app itself has authentication — Relay can't add it for you unless you pick forward-auth." Right column "Summary so far": Backend web-app · Domain app.example.com · TLS LE · HTTP-01 · Access public · Protection rate-limit · exploits · noindex · "You can change any of this later from the host's drawer." Footer Step 3 of 4 · ← Back · Review →.
Step 4 "Review & apply · Two configs change. Both are validated before anything reloads." Two code cards: Reverse proxy "+1 host" "nginx -t ✓" (server block) and Load balancer "+1 frontend" "haproxy -c ✓" (`frontend fe-web-app bind 127.0.0.1:10080 mode http option forwardfor default_backend web-app` `# backend web-app unchanged`). "What happens when you apply": 1 Request certificate (HTTP-01, ~30 s) · 2 Add HAProxy frontend, hot-reload (0 dropped connections) · 3 Add nginx host, reload, health-check for 10 s — auto-rollback on failure · 4 Saved as config version v143. Checkbox "Open https://app.example.com in a new tab when done". Footer ← Back · Add to pending · Apply now.

## 26 First-run wizard steps 2–3 (stepper Admin account · Network · Done)
Step 2 "Network check · Relay probed your environment. Fix anything red before exposing hosts publicly." Checklist: ✓ Ports 80 & 443 are free "nginx will bind both" · ✓ LAN detected · 192.168.1.0/24 "Used for the default lan-only access list" Edit · ✓ Docker socket found · 23 containers "Discovery enabled, auto-create off" · ⚠ Public IP 203.0.113.7 · port 443 not reachable from outside "Forward 80/443 on your router, or use DNS-01 certs and a VPN instead" Recheck · ✓ HAProxy engine "Installed · starts when you create the first backend". Field "Admin UI domain (optional)" proxy.home.lan "Relay creates a proxy host for itself with a LAN-only access list." Step 2 of 3 · ← Back · Continue →.
Step 3 "Relay is ready · nginx running · admin jonas · 2FA enrolled". "Pick a first task": Create hosts from Docker (23 containers found · 6 look like web apps) → · Add a proxy host manually (Domain → IP:port, TLS in one screen) → · Import from Nginx Proxy Manager (Hosts, certs and access lists are converted) → · Set up a wildcard certificate (Connect Cloudflare and request *.home.lan) →. Footer "Skip — go to the dashboard" · primary "Create hosts from Docker →".

## 27 Hosts table view
Same header as 02 with Grid|Table toggle on Table, "Columns ▾". Columns: checkbox | Domain ↑ | Upstream | TLS | Access | Features | Req · 24h | Updated | ···. Rows e.g. home.lan · http://10.0.0.10:3000 · LE · 88d · lan-only · ws · h2 · 42k · 2d; jellyfin.home.lan upstream shows " · 502" in red; app.example.com · "lb → web-app"; old-wiki disabled greyed. Floating dark bulk bar: "3 selected | Attach access list ▾ | Change certificate ▾ | Enable | Disable | Delete… | ×".

## 28 Drawers / dialogs
- 28a Edit access list "family": tabs IP rules | Basic auth | Hosts · 3. Toggle "Require username & password · Browser prompt · applied after IP rules pass". Users table Username | Password | Last used: jonas •••••••• Reset 2 h ago × · mira · grandma · new row "tv-room" with password field + Generate + Add. "+ Add user · Import htpasswd · Export". Realm text "Family only". Toggle "Satisfy any · IP match OR password". Note "Passwords stored bcrypt-hashed". Cancel · Save to pending.
- 28b New stream: "Raw TCP/UDP port forward · no TLS, no access lists". Name Valheim · Protocol TCP|UDP|Both · Listen 0.0.0.0 "all interfaces ▾" + ports 2456-2458 "Ports free · ranges allowed" · Forward to 10.0.0.61 + port "same" · suggestion "valheim-server:2456/udp" · "or a backend ▾" · Toggle PROXY protocol "Pass client IP to upstream" · Idle timeout 10m · warning "Binding to all interfaces exposes this port to the internet if your router forwards it. Streams bypass access lists." · Config valid · Cancel · Save to pending.
- 28c Add user dialog: "Signs in with username + password; 2FA required for admins." Username alex · Email (optional) "for resets" · Role radio cards Admin "Everything, incl. users and settings" / Editor "Hosts, certs, backends · can apply" / Viewer "Read-only dashboards and logs" · Initial password brisk-otter-4471 Regenerate · checkbox "Must change on first sign-in" · Cancel · Create user.
- 28d Token created dialog: green check "Token created · Copy it now — Relay stores only a hash and can't show it again." mono token rl_mcp_… Copy · Name Home Assistant · Expires 90 days · 2026-12-13 · Scope read-only · MCP + REST · Limited to *.home.lan · web-app · checkbox "I've stored it somewhere safe" · Done.
- 29a Log detail drawer: header status 502 badge + "GET /web/index.html" · "jellyfin.home.lan · 14:03:41.208 · req 7f2a…c19e". Actions: Block 192.168.1.24 · Add to access list ▾ · Filter by this IP · Open host. Stats: Client 192.168.1.24 · Total 3.01 s · Upstream refused · Bytes 157 B. Timing waterfall: tls 4 ms · acl 1 ms · connect → 10.0.0.42:8096 · timed out 3 s · ECONNREFUSED (red bar). Request headers code block (cookies redacted). "Same client · last 10 min" list with status, request, time.
- 29b Certificate detail drawer: "*.home.lan · Valid · 88 days · Let's Encrypt R11 · DNS-01 via Cloudflare". Actions Force renew · Download .pem · Test on host… · Revoke (red). SANs · Key ECDSA P-256 · Not before/after 2026-09-12 → 2026-12-11 · Fingerprint (SHA-256) A3:9F:…:C1:7E Copy. Chain: *.home.lan leaf → R11 intermediate · Let's Encrypt → ISRG Root X1 root · trusted. "Used by 9 hosts · Deleting requires reassigning them" chips +4. History list date · message · ok/retried.
- 29c Create hosts from Docker dialog: "6 containers expose an HTTP port. Pick the ones to proxy and adjust domains — TLS uses *.home.lan, access list lan-only." Table checkbox | Container | Domain (editable) | Upstream: loki loki.home.lan 172.18.0.9:3100 · prometheus · uptime-kuma status.home.lan 172.18.0.12:3001 · api-4 "— belongs to backend api" (disabled) · redis "— not HTTP (6379)" (disabled) · portainer. Domain pattern {name}.home.lan · checkbox Keep in sync with container changes · "3 selected · 1 apply" · Cancel · Create 3 hosts.
- 29d New frontend drawer: "A listening port and the rules that choose a backend". Name internal-http · Mode HTTP|TCP · Bind 10.0.0.1:8080 ▾ · Bind presets chips "127.0.0.1 — via reverse proxy" "LAN 10.0.0.1" "0.0.0.0 — public". Routing rules "First match wins": draggable rows [Host is ▾][api.home.lan] → [api ▾] × · [Path starts ▾][/s3/] → [minio ▾] × · otherwise → [web-app ▾] · + Add rule · hint "Host · Path · Header · Source IP · SNI (TCP)". Toggles Accept PROXY protocol "When fed by the reverse proxy" · Compression "gzip text/* and json". Generated config preview · haproxy -c passed · Cancel · Save to pending.

## 30
- 30a Keyboard shortcuts sheet (esc): Global — Command palette ⌘K · New host N · Apply pending changes ⌘⏎ · Search / filter / · This sheet ? ; Go to — Overview G O · Hosts G H · Load balancer G L · Certificates G C · Logs · Settings G G · G S ; Lists — Move · open J K · ⏎ · Select · select all X · ⌘A · Edit · delete E · ⌫ · Toggle grid / table V ; Drawers — Save to pending ⌘S · Next / previous tab ⌘] · ⌘[ · Close esc · Logs: pause tail space.
- 30b Session expired dialog: "You were signed out after 7 days. Your unsaved edit to grafana.home.lan is kept as a draft." Password for jonas · 2FA code (6 boxes) · "Or use a passkey" · Switch account · Continue.
- 30c Not allowed: "Viewers can't change settings · You're signed in as alex (viewer). Ask an admin to make you an editor, or open this page read-only." View read-only · Request access.
- 30d Host drawer SSL tab: Certificate select "cloud.home.lan · Let's Encrypt · 59 days · auto-renew ▾" + links Request new · Use *.home.lan instead · Upload custom. Toggles Force HTTPS "301 from :80" · HTTP/2 "Multiplexed over TLS" · HTTP/3 (QUIC) "UDP 443 · adds Alt-Svc header" · HSTS "Inherits global · 6 months". Upstream TLS "Upstream is https:// — verify its certificate?" segmented Verify | Skip (self-signed). Cipher profile "Global default: Intermediate" select Inherit ▾. Info: "HTTP/3 needs UDP 443 forwarded on your router in addition to TCP 443. Global toggle in Settings → General; this switch overrides it per host." Config valid · Cancel · Save to pending.
